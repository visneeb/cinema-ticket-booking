# Persist to disk
appendonly yes
appendfilename "appendonly.aof"

# Evict LRU keys when memory is full (locks should not be evicted — use noeviction for safety)
maxmemory-policy noeviction

# Log slow queries over 10ms
slowlog-log-slower-than 10000